#ifndef _SPSTDDEF_H_
#define _SPSTDDEF_H_

// bool
#define BOOL 	u32
#define FALSE	0
#define TRUE	1

#define MIN(x, y) (x) < (y) ? (x) : (y)
#define MAX(x, y) (x) > (y) ? (x) : (y)

#endif // _SPSTDDEF_H_
